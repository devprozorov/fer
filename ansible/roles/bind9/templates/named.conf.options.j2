options {
    directory "/var/cache/bind";
    recursion yes;
    allow-query { any; };

    forwarders {
{% for fw in bind9_forwarders %}
        {{ fw }};
{% endfor %}
    };

    dnssec-validation auto;
    listen-on-v6 { any; };
};
